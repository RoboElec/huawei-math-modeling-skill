# 编程手质检清单

- [ ] 只读取输入和建模手两个 Markdown 产物，未覆盖它们。
- [ ] 仅检查所选模型和功能需要的 Python 包或 MATLAB 工具箱。
- [ ] Python/MATLAB 代码已真实运行，退出状态和异常已检查。
- [ ] 约束、边界、量纲和验证指标正确。
- [ ] 求解稳健性已排查：无溢出/NaN/inf 残留；量纲已归一化；病态矩阵已换用 `lstsq`/`pinv`/正则化；优化器变量尺度已对齐；随机算法多初始值或多种子运行并报告最优值分布；迭代法已检查收敛；大规模数据已评估内存/时间；求解超时已有降级策略；结果通过边界 case sanity check。
- [ ] 在全量计算和正式出图前已执行 `P1`；正常路径返回 `PASS`，若两轮耗尽后继续则必须有用户显式 `HUMAN_OVERRIDE:P1` 和风险记录。
- [ ] 随机种子和关键参数显式记录。
- [ ] 输入文件 SHA-256、版本和唯一复现命令已写入 `results/复现清单.json`。
- [ ] 绘图前已核对实际行列、表头、缺失、分组样本量、分布、异常值和单位；删行、聚合或抽样均有数量与理由。
- [ ] 每张候选图已有核心结论、所属类别、证据/面板映射、统计口径和最终尺寸；图型与变量语义匹配。
- [ ] 每张图已选择叙事原型并声明主面板；不同证据权重或纵横比的面板未机械等分，互不支撑的结果未堆进同一画布。
- [ ] 已从题目分析报告列出全部子问题并规范为 `q1…qN`；每个子问题的核心结论都有图、表、关键数值、解析推导或可复现输出中的至少一种证据。只生成对论证有价值的图，不要求固定三类/固定总数。
- [ ] 已按 `tools/figure/references/chart-types/flowchart.md` **生成必须的建模流程图**：总体图覆盖 输入→子问题依赖→模型→求解→验证→输出，每个子问题被总体图或子问题图覆盖；节点与连线对应真实模型/代码，未替代结果图，也未计入三类候选图配额；SVG 与 ≥300 DPI PNG 成对导出。
- [ ] 图采用克制的 Nature/SCI 出版风格基线，无默认网格、装饰性 3D、`rainbow`/`jet` 色图或无说明的误导尺度；官方模板要求已优先应用。
- [ ] 同一变量跨图颜色语义一致，并使用色觉友好颜色及线型/标记等冗余编码。
- [ ] 第一视觉焦点是核心数据关系，不是长标题、大图例、密集纹理、冗余 colorbar 或大片空白；完整论述已移到图注。
- [ ] 稠密曲线未逐点使用大标记；图例采用直接标注、单个共享图例或合理分面，面板内没有相同语义的重复图例。
- [ ] 柱状图从零开始；少量点估计优先使用点图/区间图；离散或样本量不稳定的经验率没有被折线伪装成连续趋势。
- [ ] 标题、坐标轴、单位、图例、colorbar、数据范围和多面板编号正确；已标数值的 2×2 小矩阵没有冗余 colorbar，图例与标注不遮挡数据。
- [ ] 置信区间和统计量由代码计算，无硬编码 p 值；图注说明 n、中心统计量、区间/离散程度、检验和校正。
- [ ] SVG 与至少 300 DPI PNG 成对保存，SVG 文字可编辑，HTML 相对路径正确。
- [ ] 导出未用 `bbox_inches="tight"` 偷换最终尺寸；PNG 元数据中的实际英寸尺寸与图表契约一致。
- [ ] `check_figure.py --strict` 与 `figure_audit.py ... --questions q1 q2 ... qN --strict` 已检查实际存在的正式图并返回 0；只有官方或用户明确要求固定类别/数量时才启用 `--require-category-check`。
- [ ] 已实际打开最终 PNG 和 `_qa` 灰度预览，在论文预计尺寸下用一句话复述第一视觉结论，并检查中文/符号缺字、裁切、重叠、灰度可辨、坐标截断、主次面板和跨面板一致性；问题已通过修改源代码后重跑解决。
- [ ] 所有新文件位于 `PROJECT_ROOT`，未写入 `SKILL_ROOT`。
- [ ] 代码、结果、证据图表和复现清单冻结后已执行 `P2`；正常路径返回 `PASS`，若两轮耗尽后继续则必须有用户显式 `HUMAN_OVERRIDE:P2` 和风险记录。
- [ ] `P1`、`P2` 通过后，影响合同、关键数值或复现链的修改均已按作用域复验；若使用 `HUMAN_OVERRIDE`，失败证据和接受风险已记录。
