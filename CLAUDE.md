# CLAUDE.md — Competition-Safe Math Modeling

本仓库的比赛版目标：保持可复现、可验证和证据链完整，同时避免 Subagent 死循环和过度返工。

## 最高优先级

1. 人类队员是模型、结果和提交版本的最终决策者。
2. 当届官方硬规则、数学正确性、关键复现、主张—证据一致性优先于 reviewer 偏好。
3. 可提交完整版本优先于无限局部优化；Checkpoint V1 形成后不得破坏唯一可提交版本。
4. 不把复杂模型、更多图、更多实验或更长论文默认视为更高质量。

## Subagent 硬限制

- M1/P1/P2/W1/W2 每个门禁默认最多 2 次独立质检。
- 第 1 次 discovery review：完整发现问题并为 P0/P1 分配稳定问题 ID。
- 第 2 次 regression review：只复查上一轮 P0/P1 和修正直接引入的新 P0。
- 第二次新发现的普通 P1/P2 默认不阻塞，也不得自动开启第 3 次 review。
- 第二次仍失败时停止自动返工，标记 AUTO_RETRY_EXHAUSTED，并等待用户决定。
- 美化、额外实验、更多图、措辞偏好、非必要创新默认属于 P2，不得阻塞。

## 人工冻结

- MODEL_FROZEN：禁止自行改模型体系、核心假设、目标函数和关键约束。
- RESULTS_FROZEN：禁止自行改会改变关键结果的数据、参数或权威代码。
- PAPER_FROZEN：只允许修复官方硬约束、确定性错误和提交阻塞问题。
发现新的 P0 时先报告并请求解冻，不得自动解冻。

## 建模原则

- 简单且正确、可解释、可验证的模型可以作为主模型或基线；不得为了“创新感”无必要堆叠算法。
- 不为通过内部检查制造冗余图、冗余敏感性分析或无实际价值的附加模型。

## 镜像维护

根目录 Skill 与 `dsh-plugin/math-modeling-agent/skills/math-modeling/` 是镜像副本；控制层规则修改后必须同步两处。